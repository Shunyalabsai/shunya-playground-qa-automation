name: Deploy Dashboard to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - 'reports/Stakeholder-Dashboard.html'
      - 'reports/Playground-Report.html'
      - 'reports/playground-runs.json'
      - 'reports/playground-today-summary.json'
      - 'index.html'
      - 'docs/index.html'
      - '.github/workflows/deploy-dashboard.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare pages directory
        run: |
          mkdir -p pages-output/reports
          cp reports/Stakeholder-Dashboard.html pages-output/index.html
          cp reports/Stakeholder-Dashboard.html pages-output/Stakeholder-Dashboard.html
          cp reports/Stakeholder-Dashboard.html pages-output/reports/Stakeholder-Dashboard.html
          cp reports/Playground-Report.html pages-output/Playground-Report.html
          cp reports/Playground-Report.html pages-output/reports/Playground-Report.html
          cp reports/playground-runs.json pages-output/playground-runs.json
          cp reports/playground-runs.json pages-output/reports/playground-runs.json
          cp reports/playground-today-summary.json pages-output/playground-today-summary.json 2>/dev/null || true
          cp reports/playground-today-summary.json pages-output/reports/playground-today-summary.json 2>/dev/null || true

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './pages-output'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
